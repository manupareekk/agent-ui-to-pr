# Placeholder workflow: wire your analyzer + gh CLI / GitHub App here.
# Typical flow: cron or workflow_dispatch → query warehouse → open PR with config change.
name: experiment-decision

on:
  workflow_dispatch: {}

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: |
          echo "Replace this job with:"
          echo "  - export metrics from PostHog/Statsig/BigQuery"
          echo "  - run decision rules (primary KPI + guardrails)"
          echo "  - gh pr create ... updating config/experiment-defaults.json"
          echo "See docs/PR_BOT.md"
